#!/usr/bin/env python

import sys
import os
import ecmb

file = sys.argv[1]
basename = os.path.splitext(file)[0]

m=ecmb.Molec()
m.load_pdb( file )
m.guess_z()
m.save_crd( basename + ".crd" )
m.info_seq( basename + ".seq" )
